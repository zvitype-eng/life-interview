# Synthesis Pass Log

Dated entries from the Synthesis Pass — the generative counterpart to the coverage sweep. Runs every 10 answered questions, at session end, and after a major reveal.

Each entry: connections and repetitions noticed; hypotheses added (as `[H]`, append-only); anything flagged for `scenes.md`.

---

*(none yet)*
